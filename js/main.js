// Create grapes js object with some configuration
const editor = grapesjs.init({
    container: "#editor",
    fromElement: true,
    width: "auto",
    storageManager: false,
    plugins:["gjs-preset-webpage"],
    plugins0pts:{
        "gjs-preset-webpage":{
            
        }
    }
})